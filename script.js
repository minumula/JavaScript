/*
function passExam(name, score){
    const pass = 71;
    const fail = 35;

    if (score >= 70) { 
        console.log(name + 'has passed with ' + score);
    }
    else if (score <= 35) {
        console.log(name + 'has failed with ' + score);
    }
}

passExam('Praveen',105);
*/

// const multiply = function(x,y){
//     const z = x * y;
//     return z;
// }


// console.log (multiply(10,5));

const multiply = (x,y) => x * y;
    

console.log (multiply(10,5));